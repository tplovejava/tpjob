server:
  port: 7000
spring:
  application:
    name: tpjob-security
  cloud:
    config:
      uri: http://localhost:5000/        #Config server的uri
      profile: dev                       #指定的环境
      label: master                      #指定分支
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka/