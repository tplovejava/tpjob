spring:
  application:
    name: tpjob-baseinfo
  cloud:
    config:
      uri: http://localhost:5000/        #Config server的uri
      profile: dev                       #指定的环境
      label: master                      #指定分支

